<section id="destinace" class="sekce sekce-min-70">
    <h2 class="sekce__nadpis">Naše destinace</h2>
    <p class="sekce__podnadpis">Prozkoumejte země a regiony, kam s námi můžete vyrazit.</p>

    <div class="destinace-mrizka">
        <?php if (!empty($destinace_podle_statu)): ?>
            <?php foreach ($destinace_podle_statu as $stat => $mesta): ?>
                <div class="destinace destinace-card">
                    <h3 class="destinace-card__nadpis">
                        <a href="/zajezdy?stat=<?= urlencode($stat) ?>" class="destinace-card__stat-odkaz"><?= htmlspecialchars($stat) ?></a>
                    </h3>
                    <ul class="destinace-card__mesta-list">
                        <?php foreach ($mesta as $mesto): ?>
                            <li class="destinace-card__mesto-polozka">
                                <span>🌴</span>
                                <a href="/zajezdy?destinace=<?= $mesto['id_destinace'] ?>" class="destinace-card__mesto-odkaz">
                                    <?= htmlspecialchars($mesto['nazev_mesta']) ?>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <p class="karty-empty">
                Žádné destinace nebyly nalezeny.
            </p>
        <?php endif; ?>
    </div>
</section>
