<?php
$jePrihlasen = !empty($prihlasenyUzivatel);
$jmenoUzivatele = $jePrihlasen ? htmlspecialchars($prihlasenyUzivatel["jmeno"] . " " . $prihlasenyUzivatel["prijmeni"]) : "";
?>

<div class="sekce">
    <div style="max-width: 1200px; margin: 0 auto;">
        
        <div style="margin-bottom: 2rem;">
            <h1>Výsledky pro: <?= htmlspecialchars($hledanyNazev) ?></h1>
            <p><strong>Termín:</strong> <?= htmlspecialchars($termin) ?> (na 7 dní) | <strong>Počet osob:</strong> <?= htmlspecialchars($dospeli) ?></p>
        </div>

        <?php if (!empty($chyba)): ?>
            <div style="background: #fff3f3; color: #d32f2f; padding: 1rem; border-radius: 8px; border-left: 4px solid #d32f2f; margin-bottom: 2rem;">
                <strong>Upozornění:</strong> <?= htmlspecialchars($chyba) ?>
                <?php if (strpos($chyba, '403') !== false || strpos($chyba, 'You are not subscribed') !== false): ?>
                    <p style="margin-top: 0.5rem; font-size: 0.9em;">(Nezapomeňte na RapidAPI kliknout na tlačítko "Subscribe to Test" u daného API pro aktivaci klíče.)</p>
                <?php endif; ?>
            </div>
        <?php endif; ?>

        <?php if (!empty($hotely)): ?>
            <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem;">
                <?php foreach ($hotely as $hotel): ?>
                    <?php 
                        // Ošetření různých struktur podle konkrétního Booking API z RapidAPI
                        $nazev = isset($hotel['property']['name']) ? $hotel['property']['name'] : (isset($hotel['name']) ? $hotel['name'] : 'Neznámý hotel');
                        
                        $foto = '';
                        if (isset($hotel['property']['photoUrls'][0])) {
                            $foto = $hotel['property']['photoUrls'][0];
                        } elseif (isset($hotel['photoMainUrl'])) {
                            $foto = $hotel['photoMainUrl'];
                        } elseif (isset($hotel['image_url'])) {
                            $foto = $hotel['image_url'];
                        } else {
                            $foto = '/obrazky/placeholder.jpg'; // Pokud fotka chybí
                        }
                        
                        $cena = isset($hotel['property']['priceBreakdown']['grossPrice']['value']) ? $hotel['property']['priceBreakdown']['grossPrice']['value'] : (isset($hotel['price']) ? $hotel['price'] : 'Cena na vyžádání');
                        $mena = isset($hotel['property']['priceBreakdown']['grossPrice']['currency']) ? $hotel['property']['priceBreakdown']['grossPrice']['currency'] : 'CZK';
                        
                        $hodnoceni = isset($hotel['property']['reviewScore']) ? $hotel['property']['reviewScore'] : (isset($hotel['review_score']) ? $hotel['review_score'] : '');
                    ?>
                    <div style="background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.05); display: flex; flex-direction: column;">
                        <img src="<?= htmlspecialchars($foto) ?>" alt="Foto hotelu" style="width: 100%; height: 200px; object-fit: cover;">
                        <div style="padding: 1.5rem; display: flex; flex-direction: column; flex-grow: 1;">
                            <h3 style="margin-top: 0; margin-bottom: 0.5rem; font-size: 1.25rem; color: var(--barva-modra);"><?= htmlspecialchars($nazev) ?></h3>
                            
                            <?php if ($hodnoceni): ?>
                                <p style="color: #ff9800; font-weight: bold; margin: 0 0 1rem 0;">★ <?= htmlspecialchars($hodnoceni) ?> / 10</p>
                            <?php endif; ?>
                            
                            <div style="margin-top: auto; display: flex; justify-content: space-between; align-items: center;">
                                <span style="font-size: 1.2rem; font-weight: 700; color: var(--barva-text);"><?= htmlspecialchars(is_numeric($cena) ? number_format($cena, 0, ',', ' ') : $cena) ?> <?= htmlspecialchars($mena) ?></span>
                                <button class="tlacitko tlacitko--primarni" style="padding: 0.5rem 1rem;">Rezervovat</button>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php elseif (empty($chyba)): ?>
            <div style="text-align: center; padding: 3rem; background: #f5f7fa; border-radius: 12px;">
                <h2>Bohužel jsme nenašli žádné volné hotely.</h2>
                <p>Zkuste změnit termín nebo vybrat jinou destinaci.</p>
                <a href="/" class="tlacitko tlacitko--primarni">Zpět na vyhledávání</a>
            </div>
        <?php endif; ?>

    </div>
</div>
